[phases.install]
cmds = [
  "mkdir -p /var/log/nginx && mkdir -p /var/cache/nginx",
  "chmod -R 777 /app/src/data",
  "sed -i 's|try_files $uri $uri/ =404;|try_files $uri $uri/ /index.php?path=$uri&$args;|g' /assets/nginx.template.conf"
]